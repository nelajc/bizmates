1. Build docker image using Dockerfile in same path
command: docker build --force-rm -t registry.bizmates.com/docker/laravel:prod .

2. Run micro-services container using docker-compose
command: docker-compose up -d
